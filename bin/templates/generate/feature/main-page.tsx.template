import React from 'react';
import { PageHeader } from '@bloomneo/uikit';
import { {{FeatureName}}Card } from '../components/{{FeatureName}}Card';

/**
 * Pages own their header and return plain content — the layout route that
 * renders this page supplies the surrounding chrome.
 */
export const {{FeatureName}}Page: React.FC = () => {
  return (
    <div className="space-y-6">
      <PageHeader
        title="{{FeatureName}}"
        description="Main {{featureName}} page."
      />

      <{{FeatureName}}Card
        title="{{FeatureName}} feature"
        description="Complete feature implementation with components and pages"
        onAction={() => console.log('{{FeatureName}} action triggered')}
      />
    </div>
  );
};
