import React from 'react';
import { PageHeader, Card, CardContent, CardHeader, CardTitle, Button } from '@bloomneo/uikit';

export const {{FeatureName}}ListPage: React.FC = () => {
  return (
    <div className="space-y-6">
      <PageHeader
        title="{{FeatureName}} list"
        actions={<Button>Add new {{featureName}}</Button>}
      />

      <div className="grid gap-4">
        {Array.from({ length: 3 }, (_, i) => (
          <Card key={i}>
            <CardHeader>
              <CardTitle>{{FeatureName}} item {i + 1}</CardTitle>
            </CardHeader>
            <CardContent>
              <p className="text-muted-foreground">
                Sample {{featureName}} item content goes here.
              </p>
            </CardContent>
          </Card>
        ))}
      </div>
    </div>
  );
};
