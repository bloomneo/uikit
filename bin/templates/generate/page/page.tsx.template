import React from 'react';
import {
  PageHeader,
  Card,
  CardContent,
  CardDescription,
  CardHeader,
  CardTitle,
  Button,
} from '@bloomneo/uikit';

/**
 * A page owns its own header and returns plain content. The surrounding
 * chrome (sidebar, topbar, auth guard) belongs to the layout route that
 * renders this page through an <Outlet />, not to the page itself.
 */
export const {{FeatureName}}Page: React.FC = () => {
  return (
    <div className="space-y-6">
      <PageHeader
        title="{{FeatureName}}"
        description="Welcome to your new {{featureName}} page."
        actions={<Button>Primary action</Button>}
      />

      <Card>
        <CardHeader>
          <CardTitle>{{FeatureName}} content</CardTitle>
          <CardDescription>
            Customize this page with UIKit components and your own content.
          </CardDescription>
        </CardHeader>
        <CardContent className="space-y-4">
          <p className="text-muted-foreground">
            Start building your {{featureName}} feature here.
          </p>

          <div className="grid gap-4 md:grid-cols-2">
            <Card>
              <CardHeader>
                <CardTitle className="text-lg">Feature 1</CardTitle>
              </CardHeader>
              <CardContent>
                <p className="text-sm text-muted-foreground">
                  Add your first {{featureName}} feature here.
                </p>
              </CardContent>
            </Card>

            <Card>
              <CardHeader>
                <CardTitle className="text-lg">Feature 2</CardTitle>
              </CardHeader>
              <CardContent>
                <p className="text-sm text-muted-foreground">
                  Add your second {{featureName}} feature here.
                </p>
              </CardContent>
            </Card>
          </div>
        </CardContent>
      </Card>
    </div>
  );
};

export default {{FeatureName}}Page;
